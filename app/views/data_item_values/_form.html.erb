<%= form_for(@data_item_value) do |f| %>
  <% if @data_item_value.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@data_item_value.errors.count, "error") %> prohibited this data_item_value from being saved:</h2>

      <ul>
      <% @data_item_value.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :data_item_id %><br />
    <%= f.number_field :data_item_id %>
  </div>
  <div class="field">
    <%= f.label :current_value %><br />
    <%= f.text_field :current_value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
