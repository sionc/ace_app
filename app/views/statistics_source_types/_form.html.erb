<%= form_for(@statistics_source_type) do |f| %>
  <% if @statistics_source_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@statistics_source_type.errors.count, "error") %> prohibited this statistics_source_type from being saved:</h2>

      <ul>
      <% @statistics_source_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
